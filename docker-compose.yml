services:
  postgres:
    image: ${POSTGRES_IMAGE:-ghcr.io/mildman1848/postgresql:18.4-mldm4}
    restart: unless-stopped
    environment:
      PUID: ${PUID:-1000}
      PGID: ${PGID:-1000}
      TZ: ${TZ:-Europe/Berlin}
      POSTGRES_USER: ${POSTGRES_USER:-silo}
      FILE__POSTGRES_PASSWORD: /run/secrets/postgres_password
      POSTGRES_DB: ${POSTGRES_DB:-silo}
    volumes:
      - ./data/postgres:/config
    secrets:
      - postgres_password
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $${POSTGRES_USER:-silo} -d $${POSTGRES_DB:-silo} -h 127.0.0.1"]
      interval: 5s
      timeout: 3s
      retries: 20

  redis:
    image: ${CACHE_IMAGE:-ghcr.io/mildman1848/valkey:9.0.4-mldm1}
    restart: unless-stopped
    environment:
      PUID: ${PUID:-1000}
      PGID: ${PGID:-1000}
      TZ: ${TZ:-Europe/Berlin}
    volumes:
      - ./data/valkey:/config
    healthcheck:
      test: ["CMD-SHELL", "valkey-cli ping | grep -q PONG"]
      interval: 5s
      timeout: 3s
      retries: 20

  silo:
    image: ${IMAGE_NAME:-ghcr.io/mildman1848/silo-server}:${IMAGE_TAG:-git-881c968-mldm1}
    restart: unless-stopped
    environment:
      PUID: ${PUID:-1000}
      PGID: ${PGID:-1000}
      TZ: ${TZ:-Europe/Berlin}
      MODE: ${MODE:-integrated}
      FILE__SECRET_KEY: /run/secrets/silo_secret_key
      FILE__POSTGRES_PASSWORD: /run/secrets/postgres_password
      SILO_DB_HOST: postgres
      SILO_DB_PORT: "5432"
      SILO_DB_NAME: ${POSTGRES_DB:-silo}
      SILO_DB_USER: ${POSTGRES_USER:-silo}
      SILO_DB_SSLMODE: disable
      REDIS_URL: redis://redis:6379
      SILO_PLUGIN_CACHE_DIR: /config/plugins
      POSTGRES_TUNE: ${POSTGRES_TUNE:-off}
      SILO_PUBLIC_URL: ${SILO_PUBLIC_URL:-}
      SILO_TRUSTED_PROXIES: ${SILO_TRUSTED_PROXIES:-}
    ports:
      - "${PORT:-8090}:8080"
      - "${JF_PORT:-8096}:8096"
      - "${ABS_PORT:-13378}:13378"
    volumes:
      - ./config:/config
      - ./transcode:/transcode
      # Media libraries are mounted individually so the Silo admin UI can use
      # stable, explicit container paths per media type. Keep them read-only.
      - type: bind
        source: ${MEDIA_MOVIES_PATH:?Set MEDIA_MOVIES_PATH in .env}
        target: /media/movies
        read_only: true
        bind:
          create_host_path: false
      - type: bind
        source: ${MEDIA_TV_PATH:?Set MEDIA_TV_PATH in .env}
        target: /media/tv
        read_only: true
        bind:
          create_host_path: false
      - type: bind
        source: ${MEDIA_MUSIC_PATH:?Set MEDIA_MUSIC_PATH in .env}
        target: /media/music
        read_only: true
        bind:
          create_host_path: false
      - type: bind
        source: ${MEDIA_AUDIOBOOKS_PATH:?Set MEDIA_AUDIOBOOKS_PATH in .env}
        target: /media/audiobooks
        read_only: true
        bind:
          create_host_path: false
      - type: bind
        source: ${MEDIA_EBOOKS_PATH:?Set MEDIA_EBOOKS_PATH in .env}
        target: /media/ebooks
        read_only: true
        bind:
          create_host_path: false
      - type: bind
        source: ${MEDIA_MANGA_PATH:?Set MEDIA_MANGA_PATH in .env}
        target: /media/manga
        read_only: true
        bind:
          create_host_path: false
      - /proc/meminfo:/host/proc/meminfo:ro
    secrets:
      - silo_secret_key
      - postgres_password
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy

secrets:
  silo_secret_key:
    file: ${SECRET_KEY_FILE:-./secrets/silo_secret_key}
  postgres_password:
    file: ${POSTGRES_PASSWORD_FILE:-./secrets/postgres_password}
